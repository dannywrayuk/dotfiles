Git commit push	git-add-commit-push
Git force push	git push force --no-verify
Git status	    git status
Git rebase main	git checkout $(git main-branch) && git pull && git checkout - && git rebase $(git main-branch) -i
